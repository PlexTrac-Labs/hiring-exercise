const AUTH_TOKEN = "auth_token";
const CURRENT_USER_ID = "current_user_id";
const TOP_BAR_HEIGHT = 60;
const LIST_MIN_WIDTH = 400;

export { AUTH_TOKEN, CURRENT_USER_ID, TOP_BAR_HEIGHT, LIST_MIN_WIDTH }